# Pierre Rousseaux — academic website

Static academic website for GitHub Pages.

## Before switching the custom domain
1. Test all pages at the GitHub Pages URL.
2. Add the real CV as `files/CV_Pierre_Rousseaux.pdf`.
3. Add any desired trade-dependency figures under `assets/images/`.
4. Only after validation, configure the custom domain and DNS.
